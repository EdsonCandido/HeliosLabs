services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: frontend-landpage

    restart: unless-stopped

    networks:
      - proxy

networks:
  proxy:
    external: true
